# SDD ledger — plan: docs/archive/2026-07-frontend-rebuild/plans/2026-08-01-frontend-rebuild-m2-auth.md

Worktree: /Users/amankumarshrestha/LapenInns Project/NabaPresence/.worktrees/m2-auth (branch frontend-rebuild-m2-auth, from main @ 85d8a05)
Baseline at abf58b3: typecheck green, unit+components 288 passed / 159 skipped.
Pre-flight plan scan: clean. Task 2's four edits are the only sanctioned protected-path changes.
Carry-forwards this milestone must close (from M1 ledger): sign-in integration test re-enable (T4), apiFetch 204 handling (T3).
Task 1: fix round 1/5 dispatched (2 Important: caps-lock hint replaced describedBy checklist — corrects the brief's own XOR snippet, controller-authorised; checkPasswordRules missed schema max(128))
Task 1: fix round 1/5 (2 addressed, 0 open; commits 1f1ba5f..9e8bf7d)
Task 1: complete (commits abf58b3..9e8bf7d, review clean after 1 fix round)
Task 2: complete (commits 9e8bf7d..505b37d; controller ran gate + committed after implementer stalled; review dispatched)
Task 2: note: full integration green 46 files/159 passed/3 skipped; sync-transactions failure diagnosed as pre-existing load flake (reproduced with changes stashed)
Task 3: complete pending review (commits 505b37d..550ec0b, 308 unit+component passed) — closes M1 carry-forward: apiFetch 204 handling
Task 3: complete (commits 505b37d..550ec0b, review clean — no findings; open-redirect guard bypass-tested by reviewer)
Task 4: complete pending review (commits 550ec0b..2ac2eeb, 317 passed; M2 sign-in integration test re-enabled — closes M1 carry-forward)
Task 2: fix round 1/5 dispatched (2 Important: /resend + 429 stub coverage for BOTH auth fns; residual recipient-scoped-429 enumeration risk to be recorded not asserted away)
Task 2: note for future briefs: invitation table lives in supabase/migrations/0008_product_completeness.sql, NOT 0001_initial.sql (brief cited the wrong file; implementer found the right one)
Task 2: fix round 1/5 (2 Important + 1 Minor addressed, 0 open; commits 2ac2eeb..89716cf) — re-review PASS
Task 2: complete (commits 9e8bf7d..89716cf). RESIDUAL RISK RECORDED IN CODE: 429 surfacing branches on status only; if GoTrue has a recipient-scoped send-limit bucket, a narrow enumeration channel remains. Follow-up: branch on error.code once the deployed taxonomy is confirmed.
Task 5: complete pending review (commits 89716cf..e139408, 324 passed)
CROSS-CUTTING FIX (found by live-browser review of T4, affected T4+T5): FieldDescription rendered <p>, PasswordRequirements renders <ul> -> invalid HTML + hydration error on every create-account and reset-password view. Fixed at the primitive (field.tsx <p> -> <div>), reproduced-and-verified live before/after. commit 662032e. 326 passed.
LESSON FOR T8 + ALL FUTURE MILESTONES: jsdom cannot see hydration/HTML-nesting errors and neither can pnpm build. T8 MUST add a Playwright assertion of ZERO console errors on every auth page (both modes, both themes). Carry this guard into every later milestone's e2e.
OPEN (unattributed, dev-only): "Encountered a script tag while rendering React component" console error appears on /sign-in but NOT /home; no script tag in any auth file; likely Fast-Refresh artifact. Revisit in M9 hardening.
Task 5: complete (commits 89716cf..e139408, review Approved - Minors only). Reviewer misattributed the FieldDescription div to 1570245; verified at base 85d8a05 it was <p>, so 662032e is genuinely our fix. No double-change.
Task 5: minor (deferred): forgot-password-form omits focusField on server field errors (unreachable in practice; inconsistent with siblings)
Task 5: minor (deferred): missing-token state renders "Back to sign in" twice (its own + AuthCard footer) — brief's literal instruction; polish
Task 5: minor (deferred): no focus-to-banner for banner-only errors in either new form (role="alert" still announces; sighted-keyboard polish)
Task 5: minor -> FOLD INTO T7: a truncated/corrupted token_hash fails server zod (min 20) as invalid_request, not invalid_email_link, so the form stays with "Check the highlighted fields." and NO highlighted field and NO recovery CTA. Real dead-end-ish gap in the very behaviour this milestone fixes.
Task 6: complete pending review (commits 662032e..e267e6c, 331 passed; /invite/[token] moved into (auth) route group, URL unchanged)
Task 6: complete (commits 662032e..e267e6c, review clean — spec ✅, no Critical/Important, Approved by sonnet reviewer)
Task 6: minor (deferred): three-way verbatim-duplicated `Go to sign in` <Link> in invitation-view (404/accepted/expired) — extract to prevent drift
Task 6: minor (deferred): same-email signed-in branch in invitation-actions has no test (only mismatched-email exercised); a 6th test closes it
Task 6: minor (deferred): `Try again`→refetch() and 404 `Go to sign in` link asserted present but never clicked/exercised
Task 6: minor (deferred): accepted-invitation neutral info state renders via Alert's hardcoded role="alert" (assertive) — shared primitive components/ui/alert.tsx:32; separate ticket, out of task scope
Task 6: note: reviewer found two inaccurate report rationales (keys.ts is NOT in protected paths; AppShell has no sign-out precedent) — no code defect, but do not take this implementer's rationales at face value
Task 6: ⚠️ CARRY TO T8: no live/e2e/axe pass ran on /invite/[token]; T8's zero-console-error e2e MUST include the invite page (both modes, both themes)
Task 7: implemented (commits bce9009 sign-out + reset-token fold-in 3f99d7d; full suite 334/334 passing, gates green). DONE_WITH_CONCERNS.
Task 7: concern (for review): brief's verbatim handleSignOut is try/finally with NO catch; wired as onClick it yields an unhandled rejection on the signOut-fails path (assertions pass but vitest exit flips to 1). Implementer kept handleSignOut verbatim and added a handleSignOutClick wrapper with .catch(()=>{}). Sibling invitation-actions.tsx instead swallows inline (catch{}/finally). Reviewer to judge which is cleaner; not pre-judged.
Task 7: fold-in verified in-code: reset-password-form now gates on isUsableToken (reuses resetPasswordSchema.shape.tokenHash, no magic numbers); layout.tsx and lib/domain/auth.ts untouched.
Task 7: complete (commits f9f87c2..3f99d7d [bce9009 sign-out, 3f99d7d reset-token fold-in], review clean — spec ✅, Approved, no Critical/Important; both Co-Authored-By trailers verified).
Task 7: minor (deferred): handleSignOut(try/finally, re-throws)+handleSignOutClick(.catch) two-function split; sibling invitation-actions.tsx uses ONE function with inline catch{}. Consistency nit + latent footgun (a future direct handleSignOut wiring re-throws). Strong one-line fold-in candidate for the whole-branch fix wave.
Task 7: minor (deferred): new describe("sign out") tests in app-shell.test.tsx lack afterEach cleanup (no restoreAllMocks/unstubAllGlobals/mockReset) unlike password-reset.test.tsx; vitest isolate:true makes leakage unlikely.
Task 7: minor (deferred): truncated-token test covers only lower bound ("short"); the reused schema's max(512) upper-bound path has no dedicated test.
Task 8: implemented (commits ee28c23 a11y-name fixes + 50d6eaf auth e2e/console guard). DONE_WITH_CONCERNS: 2 of the brief's verbatim tests fail on getByRole("alert") strict-mode collision with Next's AppRouterAnnouncer. Console+pageerror guard PASSED clean on all 5 surfaces × both themes (no hydration bugs). Two real a11y bugs found & fixed in the browser: (1) password toggle aria-label collided with getByLabel("Password") -> moved name to sr-only child + added Button union arm accessibleNameFromChildren; (2) mode-toggle aria-labels contained "sign in" substring colliding with submit -> hyphenated to "Switch to sign-in"/"Switch to create-account" (+3 component test strings updated).
Task 8: CONTROLLER ADJUDICATION (plan-text conflict): the bare page.getByRole("alert") in the two failing tests collides with Next's AppRouterAnnouncer — VERIFIED at node_modules/next/dist/client/components/app-router-announcer.js:25 `announcer.role='alert'`, portaled to document.body OUTSIDE <main>. Ruling: scope those two locators to page.getByRole("main").getByRole("alert"); preserves every pinned string + intent, weakens nothing (announcer is outside main). One obviously-correct fix -> decided as careful-engineer per owner standing authorization; surfaced to owner at the milestone gate for override. Fix round 1 dispatched.
Task 8: OPEN FOR REVIEW SCRUTINY: Button primitive (M1 shared, "a11y-enforced-at-type-level") gained a 3rd union arm; sign-in-form aria-label copy changed to satisfy a Playwright substring match (vs. scoping the test locator with exact:true) — task reviewer + whole-branch review must judge both.
Task 8: fix-round-1 alert-locator scope applied + gate green (commit 6193e41, e2e 17/17); all three T8 trailers (ee28c23/50d6eaf/6193e41) verified.
Task 8: task review (sonnet) — spec ✅ (e2e verbatim + adjudicated alert scope + console guard all verified sound; AppRouterAnnouncer root cause independently re-derived; Button 3rd union arm APPROVED — existing arms still require aria-label, marker destructured out so it never hits the DOM; password-field sr-only fix APPROVED). ONE Important + one Minor.
Task 8: IMPORTANT (review): sign-in-form.tsx:205,214 aria-label hyphenation ("Switch to sign-in"/"Switch to create-account") is a VERIFIED WCAG 2.5.3 Label-in-Name regression — axe curateString strips punctuation ("sign-in"->"signin") so visible "Sign in"/"Create account" is no longer contained in the accessible name. Milestone axe gate CANNOT see it: label-content-name-mismatch ships enabled:false (experimental), not run by withTags(WCAG_TAGS). Reviewer verified empirically vs installed axe-core 4.12.1; original un-hyphenated copy passes the same check.
Task 8: CONTROLLER RULING (consistency with alert adjudication): revert both aria-labels to originals ("Switch to sign in"/"Switch to create account"); resolve the Playwright "Sign in" submit-vs-toggle collision at the LOCATOR via {name:"Sign in", exact:true} (brief-verbatim locator amendment, preserves intent, weakens nothing — same treatment as the alert scope). Revert the 3 sign-in-form.test.tsx assertion strings. Fix round dispatched.
Task 8: minor (deferred): button.tsx:92 eslint-disable on the unused marker may be redundant vs the lint config's argsIgnorePattern.
Task 8: M9 CARRY: the e2e axe gate does NOT run label-content-name-mismatch (disabled-by-default experimental); consider adding a Label-in-Name assertion in hardening — but it may surface pre-existing app-wide issues, so scope carefully.
Task 8: fix round 2/5 (1 addressed: WCAG 2.5.3 aria-label revert + 3 component-test reverts + exact:true e2e locator; 0 open; commit 6193e41..a90e0a8) — scoped re-review PASS, no new breakage, Bug-1 fixes preserved. a90e0a8 trailer verified.
Task 8: complete (commits 3f99d7d..a90e0a8, review clean after 2 fix rounds — spec ✅, Important WCAG regression fixed; all M2 exit criteria green: unit 334, e2e 17/17 auth 12/12, integration exit 0, console guard clean both themes). BOTH Playwright name collisions (alert vs Next announcer; submit vs mode-toggle) resolved at the test locator, no app copy compromised.
MILESTONE M2: all 8 tasks complete. Dispatching whole-branch review (2 parallel reviewers on opus: general + security) over merge-base 85d8a05..a90e0a8.
WHOLE-BRANCH REVIEW — GENERAL (opus): Ready to merge YES, no Critical/Important-blocking. Independently confirmed both controller adjudications sound, Button 3rd-arm guarantee intact (marker never hits DOM), protected-paths clean (only the 4 sanctioned edits), no new deps, styling/structure/copy constraints met. Triaged ALL deferred minors as CARRY (T5→M9, T6 dup-link→M3/M9, T6 alert-role→dedicated ticket, T7 tests→M9, T8 lint→M9). 429 residual: acceptably deferred. Recommended folding the T7 sign-out split into THIS fix wave; promote 429 + Alert-role to tracked tickets; consider generalizing client.ts:83 (204-only short-circuit) before a later milestone relies on it.
WHOLE-BRANCH REVIEW — SECURITY (opus): Safe to merge WITH FIXES. Strengths: error copy code-mapped never provider-echoed, no dangerouslySetInnerHTML, sign-out is real server-side session DELETE, CSRF-safe (DELETE+JSON+sameSite lax), client token guard UX-only/server authoritative, invite token sha256'd, create-account has no enumeration oracle.
SECURITY FINDING 1 (Important, MERGE BLOCKER — VERIFIED BY CONTROLLER in node): sanitiseNextPath (lib/api/next-path.ts) only rejects // and /\ prefixes; a value like "/\n//evil.com" (also \t,\r, and "/\n/evil.com") PASSES the guard but the browser strips the control char at window.location.assign -> resolves to https://evil.com => post-auth open redirect (phishing). Confirmed: current guard returns the value; parse-based fix (new URL(value,"https://placeholder.invalid"); reject if origin!==placeholder) rejects all bypass + original vectors and preserves legit paths incl. query+hash. Sinks: sign-in-form.tsx (both success branches) + app/(auth)/sign-in/page.tsx:202.
SECURITY carry-forwards (NON-blocking, recorded): F2 email_not_verified sign-in path is a stronger, non-conditional enumeration oracle (real-unconfirmed -> 403+Resend button vs 401) — BUT it is anchor backend (login route unchanged this milestone), so out of M2 scope; track WITH the 429 follow-up (the "acceptably deferred" ruling now covers BOTH channels). F3 best-effort sign-out leaves a valid server session+cookie if signOut() rejects (inherent to httpOnly logout; consider a "couldn't fully sign you out" signal). F4 no Referrer-Policy/CSP/X-Frame-Options in next.config.ts (token_hash in query, invite token in path) — defense-in-depth -> M9. F5 unauth invitation disclosure via full-entropy token — acceptable, flagged.
SECURITY enumeration verdict: recorded 429 risk acceptably deferred (conditional on a non-default recipient-scoped GoTrue bucket; timing/availability-only; uniform across flows; concrete error.code follow-up). Does NOT block merge.
FIX WAVE (one fixer, base a90e0a8): (1) BLOCKER — rewrite sanitiseNextPath to parse-based origin check + regression tests (control-char vectors) in tests/components/auth-api.test.tsx; (2) FOLD-IN — consolidate app-shell handleSignOut/handleSignOutClick into ONE inline-catch function (matches invitation-actions.tsx; retires the footgun). Then ONE scoped re-review, then merge.
FIX WAVE complete (commits 62b1c4e open-redirect fix + 4a8625a sign-out consolidation; gate green: unit 335 passed/167 skipped, e2e 17/17, integration exit 0). Both trailers verified.
FIX-WAVE SCOPED RE-REVIEW (sonnet): ALL findings addressed, no new Critical/Important breakage. Open-redirect — ADDRESSED (parse-based guard, startsWith("/") kept, 4 control-char cases assert null, no rejection weakened; reviewer re-ran all 14 vectors in node). Sign-out consolidation — ADDRESSED (one inline-catch fn, handleSignOutClick removed repo-wide, onClick wired, both tests pass, guaranteed-exit preserved).
RE-REVIEW out-of-scope note (awareness, NOT blocking): sanitiseNextPath now returns normalized pathname+search+hash (percent-encodes spaces, collapses ../, backslash->slash) — byte-identical for every current `next` value, but a latent behavior diff for a future caller feeding spaces/backslashes. Harmless-to-safer (path-traversal collapse). Note for future consumers.
M2 REVIEW-CLEAN — all 8 tasks + whole-branch (general+security) + fix wave + re-review clean. Merging to main via finishing-a-development-branch. This ledger being preserved to docs/archive/2026-07-frontend-rebuild/plans/2026-08-01-frontend-rebuild-m2-auth-ledger.md so carry-forwards survive.

## CARRY-FORWARD REGISTER (for M3–M9 owners; distilled from the above)
SECURITY (track as tickets, before any GA/hardening gate):
- 429 rate-limit enumeration channel (lib/server/password-auth.ts) — branch on error.code once deployed GoTrue taxonomy confirmed. Ruling: acceptably deferred.
- email_not_verified sign-in enumeration oracle — anchor login route (unchanged in M2); normalize to generic credentials error OR consciously accept. Track WITH the 429 item; the "acceptably deferred" ruling covers BOTH.
- best-effort sign-out leaves a valid server session+cookie if signOut() rejects (inherent to httpOnly logout) — consider a "couldn't fully sign you out" signal. (app-shell + invitation-actions)
- no Referrer-Policy/CSP/X-Frame-Options (next.config.ts); token_hash in query, invite token in path — add security headers -> M9.
- unauth invitation disclosure via full-entropy token — acceptable; flagged.
DEFERRED MINORS: T5 focusField on server field errors (M9); T5 double "Back to sign in" in missing-token (M9); T5 no focus-to-banner for banner-only errors (M9); T6 triple-duplicated "Go to sign in" Link in invitation-view (M3/M9); T6 same-email invite branch untested (M9); T6 Try-again/refetch+404 link not clicked (M9); T6 Alert role="alert" on neutral info (components/ui/alert.tsx — dedicated ticket, recurs for variant=info/success); T7 sign-out tests lack afterEach cleanup (M9); T7 reset-token max(512) upper bound untested (M9); T8 button.tsx eslint-disable maybe redundant (M9).
QUALITY/ARCH: generalize lib/api/client.ts:83 (204-only short-circuit) to empty-body-on-any-2xx before a later milestone relies on it; e2e axe gate does NOT run label-content-name-mismatch (disabled-by-default) — add a Label-in-Name check in M9 but scope for pre-existing app-wide hits.
OPEN (unattributed, dev-only): "Encountered a script tag while rendering React component" on /sign-in in DEV only (absent in prod e2e) — revisit M9.
M1 CARRY-FORWARDS still open: nav prefetch={false} re-enable per route as pages ship; legacy redirects (/reviews,/overview,/analytics,/connections) need milestone owners or M9; formatDuration negative guard (M7); toast swipe untokenised + error-page landmarks (M9); two it.skip integration tests in tests/integration/routes/sign-in.test.ts re-enable at M4.
