# SDD ledger — plan: docs/superpowers/plans/2026-08-01-frontend-rebuild-m3-shell-home.md

Worktree: /Users/amankumarshrestha/LapenInns Project/NabaPresence/.worktrees/m3-shell-home (branch frontend-rebuild-m3-shell-home, from main @ d4b5b14)
Plan commits: bc2f792 (plan) + 5a57220 (plan-review edits). BASE for T1 = 5a57220.
PRE-FLIGHT / PLAN REVIEW (opus, independent) — Verdict: Ready-to-execute after 2 edits (applied). Spec coverage complete; no placeholders; type/name consistency verified; protected-path discipline clean (ZERO protected edits this milestone — consume-only); no new deps (recharts stays unused). All 3 author-flagged decisions APPROVED:
  (1) attention-list `unresolvedComplaints > 0` filter before top-5 — sound.
  (2) CLIENT-FETCH, not server-prefetch/dehydrate — CONFIRMED CORRECT: the counts/analytics routes embed inline SQL via withTenant; NO reusable lib/server service exists to prefetch from (unlike lib/server/connections.listConnections which is why the shell CAN server-prefetch connections). A §5-compliant server prefetch would need a protected-path write (new service) or duplicating route SQL in the page (violates parity §12). So client-fetch is the least-bad M3 choice; server-hydration DEFERRED TO M7.
  (3) /overview redirect under app/(dashboard)/ — sound (inherits auth-gated layout).
  Edits applied in 5a57220: both future-route <Link>s set prefetch={false} (avoids 404-prefetch breaking Chrome networkidle in T7 e2e); lucide import fixed to TriangleAlertIcon.
M3 CARRY-FORWARD (pre-recorded for M7): server-side prefetch + dehydrate/hydrate for Home per spec §5 (server-first hybrid), together with extracting a shared lib/server analytics/counts read service. M3 Home is client-fetched through Query, consistent with M1's existing client reads.
No conflicts to escalate to owner. Beginning task loop.
Task 1: complete pending review (commit 9f22fd1, per-route nav prefetch /home=true others=false; 337 tests pass incl 2 new nav tests). Review dispatched (sonnet). T2 implementer dispatched in parallel (independent files).
Task 2: dispatched (haiku; two API clients lib/api/{review-counts,analytics}.ts, complete code in brief). BASE=9f22fd1.
Task 2: complete pending review (commit 2035f5b; ReviewCounts/AnalyticsOverview/AnalyticsLocation + fetchReviewCounts/fetchAnalyticsOverview; 341 tests pass). Review dispatched (sonnet). T3 implementer dispatched in parallel (consumes T2's pinned signatures).
Task 3: dispatched (haiku; two query hooks use-review-counts/use-analytics-overview, complete code in brief). BASE=2035f5b.
Task 1: complete (commit 9f22fd1, review Approved — reviewer mutation-tested the assertion to confirm it's load-bearing). minor (deferred): /performance NAV_ITEMS entry exceeds prettier print width — cosmetic, ESLint-clean, no CI format check; sweep at T7/M9.
Task 2: complete (commit 2035f5b, review Approved — schemas verified field-by-field vs the real routes incl. nullability of averageRating/responseRate, all 9 byStatus keys always present, zod-4 strips unknown keys so the narrower schema tolerates extra route fields). No issues.
Task 3: complete pending review (commit 942fc46; useReviewCounts/useAnalyticsOverview, staleTime 30s, mirror use-connection-health; 343 tests). Review dispatched (sonnet).
Task 6: dispatched (haiku; /overview->/home redirect page, complete code in brief, independent of data layer). BASE=942fc46.
Task 3: complete (commit 942fc46, review Approved — keys/staleTime/mirror all verified, real cache-behavior tests, no issues).
Task 6: complete pending review (commit afad615; /overview->/home redirect, route in manifest, gates green). Review dispatched (sonnet).
Task 4: dispatched (sonnet; KPI cards components/home/kpi-cards.tsx — contract + pinned tests + reference impl; consumes verified T2/T3). BASE=afad615.
Task 6: complete (commit afad615, review Approved — redirect target independently re-verified, no issues).
Task 4: complete pending review (commit 64bde3d; KpiCards + NEEDS_ATTENTION_STATES, loading/zero/error states, 348 tests, no concerns — brief/pinned-test/primitive APIs agreed). Review dispatched (sonnet).
Task 5: dispatched (sonnet; AttentionList + DisconnectedBanner — contract + pinned tests + ref impl; D5/D6; both future-route Links prefetch={false}, TriangleAlertIcon). BASE=64bde3d.
Task 4: complete (commit 64bde3d, review Approved — labels/D4 states/null handling verified; reviewer read every primitive and confirmed NO block-in-<p> nesting; re-ran tests clean).
Task 5: complete pending review (commit 0930017; AttentionList + DisconnectedBanner, 355 tests, both Links prefetch={false}, TriangleAlertIcon, no concerns). Review dispatched (sonnet).
Task 7: dispatched (sonnet; home page assembly + revive/adapt home.spec.ts + zero-console-error guard both themes + FULL milestone gate). BASE=0930017.
Task 5: complete (commit 0930017, review Approved — both prefetch={false}, TriangleAlertIcon, HTML nesting clean, D5 filter/sort/slice traced by hand vs pinned tests). minor (deferred): disconnected-banner aria-hidden shorthand vs explicit; attention-list hardcodes 3 skeleton rows (no count contract).
Task 7: complete pending review (commit 103228e; home assembly + revived home.spec + zero-console-error guard both themes; FULL gate GREEN: unit 355, e2e 23/23 [foundation+home, no flake], integration exit 0). Console guard CAUGHT A REAL BUG and it was fixed: app-shell.tsx now gates {children} behind sessionReady — Home's data components (KpiCards/AttentionList/DisconnectedBanner) 401'd once each racing the anonymous local-bootstrap session cookie; implementer claims no-op in real production. T7 diff touches app-shell.tsx (33 lines) — the ONLY cross-cutting change; review must verify its production impact + no shell regression.
Env note: the m3 worktree had no .env/.env.local (gitignored, not carried by worktree); T7 implementer copied both from the main checkout to run the gate locally — nothing tracked/committed.
Review dispatched (sonnet, focus on the app-shell change).
Task 7: review (sonnet) — Approved. app-shell.tsx sessionReady gate VERIFIED genuine no-op in production (traced layout.tsx allowAnonymous → in prod AppShell only renders with non-null session, redirect-before-mount otherwise; useSessionReady inits ready=(session!==null) so prod is synchronously true; the session===null branch is dev/e2e-only via LOCAL_BOOTSTRAP_ENABLED gated to localhost). foundation.spec unaffected (assertions sequence past the gate). Nothing else changed beyond the gate + a doc comment.
Task 7: IMPORTANT (review) → FIX ROUND 1: no unit test for the new {sessionReady?children:null} gate in tests/components/app-shell.test.tsx (all existing cases use a truthy session). Shared high-blast-radius file → add direct unit coverage. minors (deferred): report cited a nonexistent "genuinely-required bug fix exception" phrase (citation nit, edit still compliant — app-shell.tsx not protected); useSessionReady fetch has no timeout/abort (pre-existing, dev/e2e-only blast radius).
Task 7: fix round 1 dispatched (resume implementer; add app-shell.test.tsx session===null gating test). BASE for re-review = 103228e.
Task 7: fix round 1/5 (1 addressed: app-shell session-gate unit test [session=null → children absent → present post-bootstrap], mutation-verified load-bearing; 0 open; commit 103228e..373d141, test-only, app-shell.tsx byte-identical). Scoped re-review PASS — reviewer traced microtask timing to confirm the absent-assertion is genuine, no new breakage.
Task 7: complete (commits 0930017..373d141, review clean after 1 fix round).
MILESTONE M3: all 7 tasks complete + task-reviewed clean. Gate at T7: unit 356, e2e 23/23 (foundation+home), integration exit 0, console guard clean both themes. Dispatching whole-branch review (opus, general + tenant-scoping check) over merge-base d4b5b14..373d141.
WHOLE-BRANCH REVIEW (opus): Ready to merge YES. No Critical/Important. All 4 named risks PASS:
  (1) TENANT/ROLE EXPOSURE — no leak: client sends NO tenant/org/location id; both routes requireSession()+withTenant(session.organisationId) and add location_member filters for non-owner/admin; org id is server-derived from the cookie, never client-supplied; attention `/inbox?locationId=<id>` links only carry ids from the already-scoped .locations[] response. Client layer cannot widen/bypass/leak scoping.
  (2) app-shell {children} gate — GENUINE PRODUCTION NO-OP (traced: prod AppShell only renders with non-null session; useSessionReady inits ready=true synchronously, effect early-returns, no /api/session fetch, no flash/hydration mismatch; session===null branch is dev/e2e-only, localhost-gated). Just extends the same gate that already fronted StatusChip/ConnectionAnnouncer.
  (3) rendering model (client-fetch vs §5 server-first) — documented, defensible deferral: verified NO lib/server analytics/counts service exists (routes embed inline SQL via withTenant); §5 prefetch would need a protected write or SQL duplication (parity §12); server-hydration + service extraction recorded as M7 carry-forward. No M3 requirement mislabelled as M7.
  (4) protected paths — ZERO edits (diff touches only app/(dashboard) pages, components/app-shell, components/home, lib/api, lib/queries, playwright.config, docs, tests). No new deps; recharts unused.
  3 cosmetic minors only, all CARRY (none blocks merge). No fix wave needed.
M3 REVIEW-CLEAN. Merging to main via fast-forward. Ledger preserved to docs/superpowers/plans/2026-08-01-frontend-rebuild-m3-shell-home-ledger.md.

## M3 CARRY-FORWARD REGISTER (for M4/M7/M9 owners)
M4: harden useSessionReady's fetch("/api/session") with a timeout/abort (pre-existing; the {children} gate broadened its dev/e2e-only blast radius) — do it when shell work resumes in M4.
M7 (Home polish + Performance): the BIG one — server-side prefetch + dehydrate/hydrate for Home per spec §5 (server-first hybrid), which requires extracting a reusable lib/server analytics/counts read service (sanctioned protected edit) and refactoring the routes to use it (parity). Plus: charts/recharts AreaChart, prior-window deltas + non-colour cues, providerTotals.divergence trust banner, zero-filled/integer-tick axes, nulls-last table styling, fetched-at captions, "chart cards cross-link to Performance". Cosmetic: attention-list skeleton count → MAX_ROWS (avoid 3→5 layout jump); disconnected-banner aria-hidden → explicit "true"; formatDuration negative guard (if Home gains a response-time KPI).
M9: prettier format sweep (nav.tsx /performance line over print width; attention-list.tsx border-[var(--nr-…)] bracket style vs the paren shorthand used elsewhere) — no CI format-check today; also the T7 report citation nit is report-only (no code action).
