# SDD ledger — plan: docs/superpowers/plans/2026-08-01-frontend-rebuild-m4-inbox.md

Worktree: /Users/amankumarshrestha/LapenInns Project/NabaPresence/.worktrees/m4-inbox (branch frontend-rebuild-m4-inbox, from main @ bca471e)
Plan commits: 3014d3c (plan, 10 tasks) + ff72c2b (plan-review revisions). BASE for T1 = ff72c2b.
Env: .env/.env.local copied into the worktree (gitignored) so implementers can run integration/e2e locally (local Docker Supabase; DB integration needs naba_test_runtime + RUN_DB_TESTS=true per handoff).

PLAN REVIEW (opus, independent adversarial) — Verdict: Needs revisions (5 defects + polish), all applied in ff72c2b. Confirmed strong: backend shapes ALL verified matching the real routes; capabilities mirror provably correct line-by-line vs permissions.ts; protected-path discipline clean; no new deps; type/name consistency across all 10 tasks; base-ui contract sound. Adjudicated 5 author-flagged decisions:
  #1 QUEUE_STATUS_MAP (needs_reply=[new,drafted,verified,failed,rejected]; awaiting_approval/escalated/published singletons; all=server total) — APPROVE; note publish_requested is transient/All-only (added to comment).
  #2 nested capabilities:{canPublish,canEdit} on list+detail, counts byte-identical — APPROVE (minimal parity risk).
  #3 verification reasons — OBJECT→FIXED: detail route returns per-draft verdict but NO reasons; added additive review-level `latestVerification:{verdict,reasons}|null` on GET /api/reviews/[id] (join verification_result, latest draft's latest verification; cols at supabase/migrations/0001_initial.sql:235) + schema field + composer seeds panel from it + integration assertion. Satisfies §8 "reasons in the lifecycle panel" on load.
  #4 publish-clean — OBJECT→FIXED: ActionBar hardcoded isDirty:false (silent stale-publish of old draft while unsaved edits visible). Rewrote dirty-context to a subscribable store; ActionBar reads live isDirty via useSyncExternalStore, passes to evaluatePublish (disables Publish "Save your draft before publishing" while dirty).
  #5 location filter via GET /api/location-links default view ({id,name}, role-scoped) — APPROVE.
REVISIONS in ff72c2b: (1) §6 auto-select (autoSelectId helper + effect, desktop-only, non-dirty); (2) mobile detail pane (mobilePaneFor, single-pane <xl + "Back to reviews"); (3) latestVerification additive detail field; (4) publish reads live isDirty; (5) composer/action-bar tests wrap <Toaster>; (6) controlled+debounced search, publish_requested note, deleteReplyResultSchema.publishAttemptId .nullable(), Toaster already at app/layout.tsx:30 (no 2nd). Build-time reconciliation flags (base-ui Select.Value/Combobox prop names, split-pane flex ancestor) left as flagged notes.

PROTECTED-PATH TASK: Task 1 ONLY — new lib/server/capabilities.ts + ADDITIVE edits to app/api/reviews/route.ts (capabilities) and app/api/reviews/[id]/route.ts (capabilities + latestVerification). Every other protected file byte-identical. Runs the parity oracle (backend integration suite must stay green). Flag for whole-branch scrutiny (M4 analog of M2 Task 2).
DEPENDENCY CHAIN: 1→2→3 hard; then 4/5/8/9 parallelize; 6 needs 4+5; 7 needs 5+6; 10 terminal gate.
No conflicts to escalate to owner. Beginning task loop.
Task 1: dispatched (sonnet; capabilities backend + additive review-route fields, contract+reference-impl in brief; runs parity oracle). BASE=ff72c2b.
Task 1: complete pending review (commit 3580ad1; capabilities.ts new + additive capabilities on list+detail + latestVerification on detail). DONE_WITH_CONCERNS. Parity oracle GREEN (integration 168 passed/2 skipped, +3 new assertions, no regressions/47 files). Unit 356. Protected diff = exactly 3 files (capabilities.ts +75; [id]/route.ts +21; route.ts +31/-9). Concerns: (1) member/assignment mirror verified by line-comparison not fixture; (2) added `as unknown as` double-cast in list route to satisfy tsc (type-only, runtime shape proven by integration); (3) ran pnpm install (fresh worktree, no node_modules). Review dispatched (OPUS — security-critical mirror + the -9 deletions + the double-cast). T2 implementer dispatched in parallel (builds clients on the pinned, integration-verified shapes).
Task 2: dispatched (sonnet; API clients reviews/drafts/publish/approval/reply/locations, zod schemas match T1 shapes; 647-line brief). BASE=3580ad1.
Task 1: review (opus) — Approved. CAPABILITY TRUTH-TABLE VERDICT: EXACT MIRROR (all 7 role×membership cases match permissions.ts line-by-line; no tenant leak; latestVerification sub-select correct — latest draft's latest verification, nullable). Double-cast CONFIRMED benign type-plumbing (buildInboxQuery json_build_object off a not-null PK). Additive-only confirmed; parity oracle green.
Task 1: IMPORTANT (review) → FIX ROUND 1: the security-critical MEMBER branch has NO executable coverage (only owner all-true + viewer all-false short-circuits tested; member SQL path — assigned+can_publish=false→canPublish=false; not-assigned→canEdit=false; org-wide session.canPublish fallback — verified by inspection only). Add executable member-branch coverage (fast unit over reviewCapabilitiesForLocations preferred, or a member integration fixture). Test-only (not a protected change). minors (deferred): test-3 may early-return on 503 DRAFTS_ENABLED so latestVerification assertion path may not always execute; dead-defensive ?? fallbacks; canEdit doesn't reflect DRAFTS_ENABLED pause (out of contract).
Task 1: fix round 1 dispatched (resume implementer; add member-branch executable test). BASE for re-review = 3580ad1.
Task 2: complete pending review (commit 71f640d; 6 clients reviews/drafts/publish/approval/reply/locations; list+detail schemas include capabilities+latestVerification verified vs real routes; 365 passed). minor: typed mock call sites vi.fn<typeof fetch> to satisfy tsc (matches existing api-client.test convention). Review dispatched (sonnet).
Task 2: complete (commit 71f640d, review Approved — schema fidelity verified field-by-field vs real routes; list/detail carry capabilities+latestVerification; verdict/severity enums provably correct; wire mapping complete; tests genuine incl. real ApiClientError 403 propagation). minors (deferred): reply.ts publishAttemptId correctly .nullable() (brief SUMMARY said non-null — T3/consumers must NOT assume non-null); some schemas more permissive than DB NOT-NULL (conservative/safe, never rejects a valid response); no malformed_response test (coverage gap, could add in T3/6/7).
Task 1: fix round 1/5 (1 addressed: member-branch executable coverage — new tests/capabilities.test.ts unit + tests/integration/routes/review-capabilities.test.ts additions [+4]; latestVerification assertion made deterministic; 0 open; commit 71f640d..82e9bda, TEST-ONLY, capabilities.ts+routes byte-identical). Mutation check: flipping canEdit failed exactly 3 new unit tests. unit 370, integration 172 (+4), zero regressions. Re-review dispatched (sonnet).
Task 3: dispatched (sonnet; query hooks use-reviews[keepPreviousData]/use-review-detail/use-review-counts[locationId] + URL-state helper parse/serialize/toReviewsFilters/QUEUE_STATUS_MAP/queueToStatuses/autoSelectId/mobilePaneFor; consumes T2 clients; 537-line brief). BASE=82e9bda.
Task 1: fix re-review PASS — all 3 member cases hand-traced against the real code, mutation-check credibility verified (flip fails exactly 3), latestVerification determinism confirmed, test-only.
Task 1: COMPLETE (commits 3580ad1..82e9bda incl fix round; review clean after 1 fix round — EXACT-MIRROR capabilities + member-branch executable coverage mutation-verified; parity oracle green; the milestone's only protected-path task).
Task 3: complete pending review (commit cb14265; use-reviews[keepPreviousData]/use-review-detail/use-review-counts[locationId] + lib/inbox/url-state.ts [parse/serialize/toReviewsFilters/QUEUE_STATUS_MAP/queueToStatuses/hasActiveFilters/autoSelectId/mobilePaneFor]; 14/14 pinned + full suite 382 passed; Home use-review-counts intact). DONE_WITH_CONCERNS: (1) WIDENED vitest.config.ts components-project include to also match .test.ts (so the pinned .ts url-state test runs) — SHARED test-config change, review must confirm no mis-projection/double-run and unit/components separation intact; (2) vi.fn<typeof fetch> typing (matches Task 2). Review dispatched (sonnet, focus vitest.config).
Task 3: COMPLETE (commit cb14265, review Approved — vitest.config widening VERIFIED SAFE [unit project excludes tests/components/**; no mis-projection/double-run]; Home use-review-counts backward-compat confirmed; URL-state mapping traced correct field-by-field vs ReviewsFilters). minors (deferred): url-state ratings parse doesn't dedupe/validate (controlled UI, low risk); the "round-trips" pinned test only asserts serialize half (inherited from brief).
DATA-LAYER CHAIN (T1→T2→T3) COMPLETE + clean. Middle wave open.
Task 4: complete pending review (commit b0dabf9; 5 primitives Tabs/Select/Combobox/Avatar/Empty [base-ui surfaces matched, no renames] + inbox route page/loading + inbox-view/queue-tabs/review-filters/review-list/empty-states + design-system demos; 9/9 pinned, full suite 391, zero regressions; ZERO protected edits confirmed). Implementer live-browser smoke-tested /inbox with seeded data: zero console errors, auto-select/roving-tabindex/server-applied queue filtering/mobile-back/combobox roles all verified. 3 type fixes (Select onValueChange nullable; FiltersState omits only `selected`; design-system RSC serializable items) + 1 lint fix (set-state-in-effect→adjust-during-render). Review dispatched (sonnet). T5 implementer dispatched in parallel.
Task 5: dispatched (sonnet; detail pane + verification panel [renders VerificationReason[]+latestVerification] + activity timeline + React error boundary around detail pane; 583-line brief). BASE=b0dabf9.
Task 5: complete pending review (commit 784ca5b; review-detail pane + verification-panel [renders VerificationReason[]] + activity-timeline + React isolation boundary; 9/9 pinned, full suite 400; Task 4 tests green; ZERO protected edits). DONE_WITH_CONCERNS resolved: brief ref rendered locationName as bare text beside `·` so getByText failed → wrapped in <span> (markup-only, test wins). NOTE: VerificationPanel is BUILT+tested here but MOUNTED by T6's composer seeded from review.latestVerification — T6 dispatch MUST make that seeding REQUIRED (plan-revision #3), not optional. Review dispatched (sonnet).
Task 9: dispatched (sonnet; /reviews->/inbox redirect [forwards query], nav /inbox prefetch:true, useSessionReady timeout/abort, re-enable the 2 it.skip integration tests — route exists now). e2e un-quarantine (inbox/journeys) DEFERRED to T10 per brief; review-provider-races STAYS quarantined (M9). Independent of T4/T5 internals. BASE=784ca5b.
Task 4: COMPLETE (commit b0dabf9, review Approved — base-ui roles verified against INSTALLED source; roving-tabindex/auto-select/mobile-pane/3-way-empty verified; RSC boundary correct; 3 build-fixes + hooks-lint fix sound; scope clean). minors (deferred): (1) ELEVATED — inbox-view.tsx reviewsQuery.isError renders EmptyState kind="no-data" → a real fetch/500 error looks like an empty inbox; spec §9 wants error state DISTINCT (loading/error/empty/mutation-failure); WHOLE-BRANCH REVIEW MUST TRIAGE — likely fix-before-merge (add an error+retry affordance). (2) auto-select effect re-evals each render (useMemo tidier); (3) search sync-from-URL can clobber keystrokes mid-round-trip; (4) queue-tabs no TabsPanel (URL-driven; not an ARIA violation).
Task 5: COMPLETE (commit 784ca5b, review Approved — real class error boundary sibling-scoped + keyed on selected; VerificationPanel renders severity+message [omits raw code, correct]; useId heading; no HTML-nesting violations; Task 4 intact). minors (deferred): timeline key={index}; rating aria-label "1 stars" grammar (from brief); metadataSummary render (disclosed judgment).
Task 9: complete pending review (commit 4a58db2; app/reviews/page.tsx redirect [forwards query] + nav /inbox prefetch:true + useSessionReady AbortController timeout [fail-open, no deadlock] + 2 re-enabled it.skip integration tests PASS [anon→/sign-in, signed-in→200]; full gate 403 passed; M3 session-gate tests green). minor: app-shell test wrapped in QueryProvider (StatusChip needs QueryClient once gate opens) + 2-arg fetch mock. Review dispatched (sonnet).
Task 6: dispatched (sonnet; reply composer + shared useDirtyGuard hook + subscribable dirty-context store + regenerate-over-edits confirm + MOUNTS VerificationPanel seeded from review.latestVerification [REQUIRED per plan-rev #3] + live mutation; AlertDialog primitive). Milestone's most complex task. BASE=4a58db2.
Task 9: COMPLETE (commit 4a58db2, review Approved — useSessionReady FAILS OPEN verified across all 4 paths [unconditional setReady in finally; no leaked timer; no setState-after-unmount; M3 contract preserved]; redirect forwards query; prefetch flipped; integration re-enables clean; scope clean). minor: 2 unused-var lint warnings in the new app-shell test (cosmetic).
Task 6: complete pending review (commit 62ea6d1; reply-composer + shared useDirtyGuard [dedicated unit tests] + subscribable dirty-context store [imperative-read vs useSyncExternalStore subscribe] + regenerate-over-edits confirm; VerificationPanel MOUNTED+seeded from review.latestVerification [required, confirmed] + live mutation; 8/8 pinned + full suite 411; Tasks 4/5 green). DONE_WITH_CONCERNS: 4 non-behavioural gate fixups — (1) dirty-context lazy ref → useState(()=>new DirtyStore()); (2) use-dirty-guard ref mutation moved into effect; (3) Select onValueChange typed string|null; (4) test mock UseMutationResult cast widened to `any` + scoped eslint-disable (never/unknown both fail tsc on TanStack MutateOptions variance). Zero pinned-assertion changes. Review dispatched (OPUS — dirty-guard user-data-loss stakes + store subscription semantics + the `any` cast). T8 implementer dispatched in parallel.
Task 8: dispatched (sonnet; full filter set [date range/reply state/verification/publish/sync] + mobile filter Sheet + active-filter chips + Clear filters, all URL-driven via T3 url-state; extends review-filters.tsx). BASE=62ea6d1.
Task 6: review (opus) — NEEDS FIXES (scoped). Deliverables all correct+high-quality: store subscription (getSnapshot returns boolean primitive, stable subscribe, imperative readers don't re-render), VerificationPanel seeding (mutationVerification ?? review.latestVerification — required not optional, verified), composer state machine (label by drafts.length; regenerate-confirm only when dirty; body-vs-tone payload), 4 fixups sound, the `any` test cast CLEARED as benign (TanStack MutateOptions variance, runtime mock resolves DRAFT_RESULT correctly), primitives a11y-sound. DIRTY-GUARD SAFETY VERDICT: guarded paths protected (onSelect/auto-select/beforeunload/401-stash/regenerate-confirm/dialog-dismiss) BUT 3 UNGUARDED in-app nav paths in inbox-view.tsx SILENTLY LOSE a dirty draft: onFilterChange (:72-76), onQueueChange (:77-80), "Back to reviews" (:220) — each clears selected → unmounts dirty composer with NO gate/stash/confirm. Reachable in normal use (desktop filter/tab click; mobile Back). Spec §6 says "confirm on nav" — these are nav.
Task 6: IMPORTANT (review) → FIX ROUND 1: gate every `selected: undefined` transition in inbox-view.tsx behind dirtyGate() (prompt-then-proceed/abort) — onFilterChange, onQueueChange, Back-to-reviews (+ any others). minors: Save button doesn't disable on overLimit (>4096 submittable, server rejects) — add `|| overLimit`; textarea aria-label "Reply draft" vs visible "Your reply" WCAG 2.5.3 nit (from pinned test — defer). Fix round dispatched (resume T6 implementer). NOTE current HEAD is e09f847 (T8 landed on top of T6); fix applies to the CURRENT inbox-view.tsx.
Task 8: complete pending review (commit e09f847; full filter set date/reply/verification/publish/sync + mobile Sheet + active-filter chips + Clear filters, all URL-driven [only local state = Sheet open/close]; 7/7 + full suite 417; Tasks 4/5/6 green). DONE_WITH_CONCERNS: PUBLISH_STATUS/SYNC_STATUS "Failed"/"Pending" collide with VERIFICATION's same labels → getByRole checkbox multi-match; kept VERIFICATION labels, added disambiguating ariaLabel to the 3 colliding non-verification checkboxes (visible text unchanged). Review dispatched (sonnet — CHECK WCAG 2.5.3: is visible text contained in the new ariaLabel? [M2 lesson]).
CORRECTION: T6 fix was mistakenly SendMessage'd to af3b20918abef6c6a (the T9 REVIEWER) by id-confusion; it correctly REFUSED (no files touched; T9 Approved verdict stands). Re-sent to the correct T6 implementer id ac760a281926716f7. AGENT IDs for M4: T6 implementer=ac760a281926716f7; T9 reviewer=af3b20918abef6c6a.
Task 8: review (sonnet) — NEEDS FIXES. WCAG 2.5.3 verdict COMPLIANT (disambiguating aria-labels keep visible text as trailing substring: "Publish status: Failed" ⊃ "Failed"; VERIFICATION keeps plain labels; and the pinned test uses unscoped getByRole so the aria-label fix was the only option — not the M2 trap). URL-driven checkbox/radio wiring solid, chip a11y good, scope clean. IMPORTANT: date-range inputs (dateFrom/dateTo) are UNCONTROLLED — no value bound to state → a URL-active date filter renders blank inputs (not URL-driven on READ). Fix: value={state.dateFrom ? state.dateFrom.slice(0,10) : ""} (+ dateTo). Inherited from brief ref code. minors (deferred): date inputs hardcode id vs useId convention; reply-state radio aria-label redundant.
Task 8: FIX ROUND queued (bind date inputs to state) — MUST wait for T6 fix to finish first (both writers, one worktree). T8 implementer=a554cfcd5380b1409; T8 reviewer=a7e9b4d2a1d37111e.
Task 6: fix round 1/5 (commit e09f847..bdc6e5c; gated onFilterChange/onQueueChange/Back(onBackToList) + onClearFilters [4th transition — Task-8 Clear-all also drops selected; implementer flagged the scope extension for 2nd opinion] behind dirtyGate(); overLimit Save-disable; new inbox-view.test.tsx 4/4 mutation-verified RED pre-fix; reply-composer 5/5; 422 passed; Tasks 4/5/8 green + live smoke clean; no protected edits). Re-review dispatched (sonnet, confirm onClearFilters extension correct).
Task 8: fix round dispatched (resume a554cfcd5380b1409; bind date inputs value to state.dateFrom/dateTo + useId the date ids). BASE for re-review = bdc6e5c.
Task 6: COMPLETE (commits 62ea6d1..bdc6e5c incl fix round 1; review clean — dirty guard now protects ALL 4 selected-clearing nav transitions [onFilterChange/onQueueChange/onBackToList/onClearFilters], mutation-verified [strip guards → 4/4 fail]; overLimit Save-disable wired; no 5th ungated path; store/seeding/state-machine all sound). The milestone's hardest task, done.
Task 8: fix round (commit bdc6e5c..a0f1b0b; date inputs now value-bound to state.dateFrom/dateTo + useId'd; round-trip test 8/8; full suite 423; Tasks 4/5/6 green; no protected edits). Re-review dispatched (sonnet).
Task 7: dispatched (sonnet; capability-gated publish/approve/reject/delete action bar + mutation hooks use-publish-review/use-approval-decision/use-delete-reply [invalidate counts+reviews+reviewDetail]; disabled-with-reason from T1 per-review capabilities + workflow transitions + dirty store [Publish disabled while dirty]; second-approver copy; per-action pending; server-confirmed no-optimistic; error taxonomy via describeActionError/action-errors.ts [T6-created, T7-tested]; AlertDialog confirms). 695-line brief. BASE=a0f1b0b.
Task 8: COMPLETE (commits e09f847..a0f1b0b incl fix round 1; review clean — WCAG 2.5.3 compliant; ALL filters URL-driven incl. date inputs now value-bound with exact round-trip, mutation-verified). 
DATA+UI TASKS DONE: T1,T2,T3,T4,T5,T6,T8,T9 complete + review-clean. Only T7 (in flight) + T10 remain.
Task 7: complete pending review (commit b40fd30; action-bar.tsx + lib/inbox/actions.ts [evaluatePublish/Approval/Delete gating] + DropdownMenu primitive + 3 mutation hooks [invalidate reviewDetail+reviews+reviewCounts] + tests action-bar/action-errors/inbox-actions; 14 pinned + full suite 437; Tasks 4/5/6/8 green; ZERO protected edits). Publish gated: canPublish(T1) AND isAllowedReviewTransition(status,"publish_requested") AND hasVerifiedDraft AND !isDirty, each a distinct disabled reason. concerns: 2 mechanical TS-strict fixes (onPublish/onDecision/onDelete arrow-const for review-narrowing; test mock any-cast per Task 6 precedent) — no gating/assertion change. Review dispatched (sonnet — gating correctness vs server enforcement + error taxonomy).
Task 7 agent id = aff7c11260fe3b9be.
Task 7: review (sonnet) — NEEDS FIXES. Strengths: gating FSM centralised in actions.ts, no optimistic local "published" flag, second-approver copy byte-exact + server-confirmed, all 3 hooks invalidate detail+reviews+counts, TS fixes benign, scope clean. FINDINGS:
  CRITICAL #1: onPublish/onDecision only special-case awaiting_approval; a Google-moderation `rejected` outcome (executePublish returns HTTP 200 status:"rejected", RESOLVED not thrown; validated as z.string()) falls through to a "Reply published" SUCCESS toast — then the invalidated refetch shows it rejected (user-visible contradiction). Violates "no non-published outcome renders as published". FIX: branch on result.status (only actually-published → published toast; rejected → declined toast; awaiting_approval → submitted; derive statuses from lib/domain/reply-state.ts + publishing.ts).
  IMPORTANT #3: Reject fires immediately with NO AlertDialog confirm (only Delete has one) — violates the stated binding "Reject/Delete confirm via AlertDialog"; Reject reverts workflow_status→drafted + clears approval_requested_by server-side. FIX: add a Reject confirm AlertDialog (+ optional note).
  IMPORTANT #4: evaluateDelete gates on Boolean(review.reply?.body) not reply.publishStatus → "Delete published reply" (copy "removes your reply from Google") can appear during awaiting_approval/after rejected when nothing reached Google. FIX: gate on reply.publishStatus being an actually-published state (per lib/domain/reply-state.ts).
  DEFERRED → WHOLE-BRANCH TRIAGE / follow-up: IMPORTANT #2 non-publishers blocked from the submit-for-approval path the server accepts by default (org.approval_required defaults true; approval route rule#1 "non-publishers request approval") — evaluatePublish disables Publish whenever !canPublish. Real spec gap (spec envisions non-publisher requesters) BUT fixing needs a new capability signal (canRequestApproval on T1) + UX ("Submit for approval" label) — a scoped mini-feature. SURFACE AT MILESTONE GATE.
  minors (deferred): #5 describeActionError misses several reachable codes (publish_in_progress/previous_publish_failed/review_changed/verification_*/location_not_verified → generic fallback, no code leak); #6 review.verified never consulted (server 409 location_not_verified); #7 onDelete toast wrong for "cancelled" status.
Task 7: FIX ROUND 1 dispatched (resume implementer aff7c11260fe3b9be — #1 Critical + #3 + #4). BASE for re-review = b40fd30.
Task 7: fix round 1 (commit b40fd30..d882d45; #1 describeOutcomeToast maps every resolved status honestly [rejected→"Google declined this reply." error, never published]; #3 Reject AlertDialog confirm + note; #4 Delete gated on publishStatus==="published" via LIVE_ON_GOOGLE set; +new tests; full suite 446; Tasks 4/5/6/8 green; no protected edits). Re-review dispatched (sonnet).
Task 10: dispatched (sonnet; milestone e2e — un-quarantine+adapt inbox.spec/journeys.spec, zero-console-error guard /inbox both themes, per-role permission walk, publish+approver journeys, dirty-draft survival; reuse stub-bridge; LEAVE review-provider-races quarantined; FULL milestone gate + confirm M4 exit criteria). Terminal task. BASE=d882d45.
Task 7: COMPLETE (commits a0f1b0b..d882d45 incl fix round 1; review clean — gating FSM correct vs server enforcement, honest outcome toasts [rejected≠published], Reject confirm, delete gated on publishStatus; also closed a bonus bug: approve had always rendered "published" regardless of server outcome).
ALL FEATURE TASKS (T1-T9) COMPLETE + review-clean. Only T10 (terminal e2e+gate) remains.
Task 10: complete pending review (commit 1d8a1c5; MILESTONE GATE FULLY GREEN: unit 448, e2e 33/33, integration 174/174 RUN_DB_TESTS=true, typecheck/lint/build clean; console guard clean /inbox both themes; revived inbox.spec+journeys.spec [publish/approver/per-role/dirty-survival] pass; review-provider-races stays quarantined; no protected edits). SCOPE CREEP: T10 self-applied 4 component fixes + self-reviewed (need independent review): (1) WCAG AA contrast on queue-tab count badge; (2) list fetch-error → Alert+retry [= the deferred T4 elevated minor, now FIXED]; (3) roving-tabindex focus desync when dirty-guard blocks arrow-nav [onSelect returns whether it proceeded]; (4) activity-timeline React key. +regression tests. CONFIRMS T7-#2: plain non-publisher can't reach approval flow via UI (approver journey seeds two-person approval instead). Concerns flagged: hardcoded Europe/London tz fallback in list; Approve/Reject + approval-required-publish capability-parity gaps (server-enforced, non-exploitable). Review dispatched (sonnet, scrutinize the 4 bundled fixes + e2e quality).
Task 10 agent id = a5ccaa37bf7b5a84b.
Task 10: COMPLETE (commit 1d8a1c5, review Approved — 4 bundled fixes all sound [contrast math hand-verified; error-branch mutual-exclusive; focus/tabindex traced both paths; timeline key], e2e genuine no-weakening [console guard real, journeys drive real UI, per-role walk real, no skip/only], approver-journey deviation honest [two-person approval reaches same awaiting_approval path, documented]). No issues.
MILESTONE M4: ALL 10 TASKS COMPLETE + review-clean. Gate: unit 448, e2e 33/33, integration 174/174, console guard clean both themes. Dispatching whole-branch review (2 parallel opus: general + capability/tenant-security) over merge-base bca471e..1d8a1c5.
M4 DEFERRED ITEMS for whole-branch triage (BLOCKS MERGE or CARRY?):
- T7-#2 (functional, core journey): non-publishers can't submit-for-approval via UI though the server accepts it by default (approval_required defaults true; approval route rule#1). Needs canRequestApproval capability (additive T1 edit) + "Submit for approval" UX. CONFIRMED by T10 (approver journey had to seed two-person-approval instead).
- tz-fallback: review list hardcodes Europe/London (list API doesn't expose per-location timezone) — dates may be wrong-tz for other locations.
- T7 minors: describeActionError misses reachable codes (publish_in_progress/previous_*_failed/review_changed/verification_*/location_not_verified → generic fallback, no code leak); review.verified not consulted (server 409 location_not_verified); onDelete toast wrong for "cancelled".
- earlier deferred minors: T4 auto-select re-eval/search-clobber/queue-tabs-no-TabsPanel; T5 rating "1 stars" grammar; T6 textarea aria-label WCAG nit; T8 reply-state radio redundant aria-label; T2 no malformed_response test.
- SERVER-HYDRATION (spec §5) still deferred: inbox is client-fetched (like M3 Home) — extract lib/server read services + dehydrate in a later milestone.
WHOLE-BRANCH SECURITY/CAPABILITY REVIEW (opus): Safe to merge YES. Capability truth-table EXACT MIRROR (re-derived every role×membership). Gating direction: NO over-permissive gap — client gate is UX-only; every mutation server-re-authorizes (requireLocationAccess+canPublishLocation) with defense-in-depth RLS (force RLS + tenant_isolation policy on location_member/review/draft/verification_result/review_reply). Tenant isolation: NO leak (capabilities query user+org+RLS-scoped; latestVerification scoped to review's own draft; search double-scoped + parameterized). No XSS/injection (all React-escaped, no dangerouslySetInnerHTML, codes→mapped copy). 3 backend edits strictly additive; parity oracle covers them. No Critical/Important. 2 Minor-Hardening (CARRY): 401 draft-stash keyed by reviewId only → shared-device restore edge (pre-existing infra; namespace by userId or clear on sign-in) → M9; direct-publisher-in-two-person-mode button label cosmetic. T7-#2 = the SAFE over-restrictive direction (not security).
WHOLE-BRANCH GENERAL REVIEW (opus): Ready to merge YES. No Critical/Important in delivered code. 5 named risks all PASS: protected-path EXEMPLARY (exactly 3 sanctioned files under protected globs, all others byte-identical, no new deps); dirty guard all 5 selected-clearing paths gated + store getSnapshot stable (no loop); URL single-source + §5 deferral documented (not silent); action-bar gating server-ALIGNED (VERIFIED={pass,warn} matches approval route + publishing.ts) + honest outcomes; test integrity real (no .only/weakened skips, console guard real both themes, dirty-guard+capability unit tests mutation-verified). §8 contract fully delivered. T4-elevated (fetch-error→empty) RESOLVED by T10, NOT carried. TRIAGE: T7-#2 CARRY→M5 (net-new scope, not a defect; publisher+approver journeys complete; needs owner product sign-off); tz-fallback CARRY→M5/M7 (list-only, detail pane already correct-tz); all other minors CARRY. No fix wave.
M4 REVIEW-CLEAN — BOTH whole-branch reviews (general + security) Yes, no Critical/Important, no fix wave. Merging to main. Ledger preserved to docs/superpowers/plans/2026-08-01-frontend-rebuild-m4-inbox-ledger.md.

## M4 CARRY-FORWARD REGISTER (for M5/M7/M9 owners + OWNER product decision)
OWNER PRODUCT DECISION (surface at gate, target M5): T7-#2 — should approval-required orgs let NON-PUBLISHERS submit a reply FOR APPROVAL via the UI? Server already accepts it (approval route rule#1; approval_required defaults true) but the UI disables Publish whenever !canPublish, so a plain member can't initiate. Fix = additive `canRequestApproval` capability on T1 (protected) + a "Submit for approval" affordance. Not a blocker (publisher + two-person-approver journeys work); flagged by both reviewers for explicit owner sign-off.
M5/M7: tz-fallback — review LIST hardcodes Europe/London (list API doesn't expose per-location timezone; detail pane is correct). Fix needs an additive list-API tz field. Low impact (UK product).
M9 (polish/hardening): describeActionError misses reachable codes (publish_in_progress/previous_*_failed/review_changed/verification_*/location_not_verified → generic fallback, no code leak) — add specific copy; onDelete toast says "deleted" for status:"cancelled"; review.verified not consulted (server 409 location_not_verified); composer textarea aria-label "Reply draft" vs visible "Your reply" (WCAG 2.5.3 Label-in-Name); T5 rating aria-label "1 stars" grammar; T4 auto-select re-eval each render + search keystroke-clobber mid-round-trip + queue-tabs no TabsPanel; T8 reply-state radio redundant aria-label; T2 no malformed_response test; 401 draft-stash namespace by userId (shared-device edge, pre-existing).
SERVER-HYDRATION (spec §5): inbox client-fetched (like M3 Home) — deferred until a milestone extracts a reusable lib/server review read-service + dehydrates.
PROCESS NOTE for future milestones: tighten the exit-criteria protected-path diff command to `-- app/api lib/server lib/domain supabase scripts instrumentation.ts` (app/ also holds non-protected page routes, so a bare `app` overcounts). review-provider-races.spec.ts STAYS quarantined (tests deleted epoch/scope-token mechanism) — M9 decide re-author-vs-delete.
NOTE: T10 exhibited SCOPE CREEP (self-applied 4 component fixes + self-reviewed); fixes were independently task-reviewed (Approved) + whole-branch-reviewed clean — acceptable this once, but future terminal tasks should route component fixes through the normal review loop, not self-apply.
OPEN for whole-branch triage: T4 elevated minor (fetch-error renders EmptyState no-data; spec §9 wants error distinct — likely fix-before-merge).
OPEN for whole-branch triage: T4 elevated minor (reviewsQuery.isError renders EmptyState no-data → fetch error looks like empty inbox; spec §9 wants error distinct) — likely fix-before-merge.
