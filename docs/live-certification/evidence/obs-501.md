# OBS-501 evidence

## Local implementation evidence

- Timestamp (UTC): `2026-07-30T01:12:31Z`
- Runtime identity:
  `postgresql://naba_test_runtime:…@127.0.0.1:54322/postgres`
- Migration: `0009_ops_heartbeat` applied successfully; runtime role grants
  refreshed successfully.
- Test:
  `tests/integration/routes/health-alerting.test.ts` — 3 passed.
- Assertions: tenant counters reflect one failed webhook and one ambiguous
  publish; the scheduler heartbeat is written by the jobs runner; the
  `CRON_SECRET` platform scope works without a browser session; invalid monitor
  credentials return 401.

## Staging collector and scraper

**BLOCKED — requires `STAGING_BASE_URL`, deployed staging runtime/cron
credentials, and an OTLP collector target (P1 remote + P4 staging
infrastructure).**

When unblocked, record:

- deployment/run URL:
- `OTEL_EXPORTER_OTLP_ENDPOINT` destination (no credentials):
- health scraper identity and one-minute schedule:
- first successful redacted platform health payload:
- dashboard URL:

## Staging alert-firing drill

**BLOCKED — requires the staging deployment and database (P1/P4), pilot Google
connection (P2), and Pub/Sub delivery path (P3). No alert has been claimed or
simulated.**

Append timestamped observations:

| Drill | Start/end UTC | Injected condition | Alert payload or screenshot | Recovery observation | Result |
|---|---|---|---|---|---|
| Scheduler silent |  | stop scheduler for more than five minutes |  |  | BLOCKED |
| Webhook backlog |  | bad location plus revoked-token sync failure |  |  | BLOCKED |
| Ambiguous publish |  | sanctioned Google mutation ambiguity injection |  |  | BLOCKED |

### Deferred AI-401 staging smoke

**BLOCKED — `STAGING_OPENAI_KEY_AVAILABLE=no` and `STAGING_BASE_URL` is blank
(P2/P4).**

Run the four committed semantic-verifier corpus fixtures in staging, record
the model/version and redacted verdicts, then link the result from the GGL-502
evidence. Never paste the OpenAI key into this file.
