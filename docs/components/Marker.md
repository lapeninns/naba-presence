---
category: AI & Chat
---

Inline citation or annotation marker.

Use inside prose to reference a source or footnote. `MarkerIcon` is decorative and already `aria-hidden`; put the reference text in `MarkerContent`.

## Parts

Composed inside `<Marker>`, each importable from `window.NabaReview.*`:

- `MarkerContent`
- `MarkerIcon`
