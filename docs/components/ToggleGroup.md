---
category: Actions
---

A set of toggles behaving as single- or multi-select.

Set `toggleMultiple` for multi-select, otherwise it behaves as a radio set. Children are `ToggleGroupItem`.

## Parts

Composed inside `<ToggleGroup>`, each importable from `window.NabaPresence.*`:

- `ToggleGroupItem`
