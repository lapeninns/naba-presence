---
category: Forms
---

Date picker calendar.

Use inside a `Popover` for a date input, or inline for a persistent picker. Supports single, multiple, and range selection.

## Parts

Composed inside `<Calendar>`, each importable from `window.NabaPresence.*`:

- `CalendarDayButton`
