---
category: Utilities
---

Sets text direction (LTR/RTL) for descendant components.

Mount once near the app root with `direction="rtl"` for right-to-left locales. Base UI components read direction from here to flip their positioning logic.
