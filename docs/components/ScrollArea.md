---
category: Layout
---

Container with styled custom scrollbars.

Use for constrained scrolling regions. Give it an explicit height, or it will not scroll.

## Parts

Composed inside `<ScrollArea>`, each importable from `window.NabaReview.*`:

- `ScrollBar`
