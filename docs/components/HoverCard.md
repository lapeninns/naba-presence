---
category: Overlays
---

Preview card shown on hover.

Use for non-essential enrichment (profile or link previews). Never gate required information behind hover — it is unreachable on touch.

## Parts

Composed inside `<HoverCard>`, each importable from `window.NabaPresence.*`:

- `HoverCardContent`
- `HoverCardTrigger`
