---
category: Forms
---

Multi-line text field.

Same fill-based styling as `Input`. It uses `field-sizing-content` with a `min-h-16` floor, so the height follows the content automatically — **`rows` has no effect**. To change the resting height, override the floor (`className="min-h-24"`) rather than passing `rows`.
