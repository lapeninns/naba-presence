---
category: Forms
---

Single-line text field.

Styled with a tinted fill (`bg-input/50`) and a transparent border — the field is identified by its label and focus ring, not a visible outline. Always pair with `Label`. Set `aria-invalid` to surface the destructive state; do not hand-colour the border.
