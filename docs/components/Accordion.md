---
category: Layout
---

Set of collapsible sections.

Children are `AccordionItem` with `AccordionTrigger` and `AccordionContent`. Use for FAQs and progressive disclosure of peer sections.

## Parts

Composed inside `<Accordion>`, each importable from `window.NabaReview.*`:

- `AccordionContent`
- `AccordionItem`
- `AccordionTrigger`
