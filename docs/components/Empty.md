---
category: Feedback
---

Empty-state surface.

Compose `EmptyHeader` (with `EmptyMedia`, `EmptyTitle`, `EmptyDescription`) and `EmptyContent` for the primary action. Give every list a designed empty state rather than blank space.

## Parts

Composed inside `<Empty>`, each importable from `window.NabaPresence.*`:

- `EmptyContent`
- `EmptyDescription`
- `EmptyHeader`
- `EmptyMedia`
- `EmptyTitle`
