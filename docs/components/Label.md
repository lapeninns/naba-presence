---
category: Forms
---

Accessible label for a form control.

Bind with `htmlFor` matching the control's `id`. Required for every input, select, checkbox, and switch — the fill-based field styling depends on the label for identification.
