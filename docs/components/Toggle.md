---
category: Actions
---

A two-state pressable button.

Use for a single on/off control that looks like a button (bold, italic, mute). For a bound form boolean prefer `Switch` or `Checkbox`; for mutually exclusive choices use `ToggleGroup`.
