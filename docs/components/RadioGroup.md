---
category: Forms
---

Mutually exclusive choice within a set.

Always render `RadioGroupItem` children inside the group — an item outside its `RadioGroup` has no context and will not render. Use for 2–5 visible options; beyond that prefer `Select`.

## Parts

Composed inside `<RadioGroup>`, each importable from `window.NabaPresence.*`:

- `RadioGroupItem`
