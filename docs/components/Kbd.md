---
category: Data Display
---

A keyboard key.

Use in shortcut hints and command menus. `KbdGroup` renders a chord (⌘ + K).

## Parts

Composed inside `<Kbd>`, each importable from `window.NabaPresence.*`:

- `KbdGroup`
