---
category: Layout
---

Show/hide a single region.

Use for one independent disclosure. For a set of related sections use `Accordion`.

## Parts

Composed inside `<Collapsible>`, each importable from `window.NabaPresence.*`:

- `CollapsibleContent`
- `CollapsibleTrigger`
