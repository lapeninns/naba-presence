---
category: Data Display
---

Compact status or metadata label.

Use for state (Replied, Escalated) and counts. `destructive` renders as red text on a tint. Reach for semantic colour via the variant rather than hand-styling.
